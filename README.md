# experimenting with openrndr

Installation:
```bash
git clone https://github.com/DLochmelis33/openrndr-3.git
cd openrndr-3
gradlew run # or use 'Run' feature of any IDE
```

Controls:
* space - regenerate
* enter - save to file (requires folder 'saves/')
* escape - exit
